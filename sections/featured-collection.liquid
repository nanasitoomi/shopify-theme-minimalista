<div class="featured-collection"><h2>Featured Collection</h2><div class="products-grid">{% for product in collections.frontpage.products limit: 4 %}<div class="product-card"><a href="{{ product.url }}"><img src="{{ product.featured_image | img_url: "300x300" }}" alt="{{ product.title }}"><h3>{{ product.title }}</h3><p>{{ product.price | money }}</p></a></div>{% endfor %}</div></div>
